import React from "react";
import styled from "styled-components";
const Event = () => {
	return <StyledDiv>event</StyledDiv>;
};

export default Event;

const StyledDiv = styled.div`
	border: solid green;
`;
